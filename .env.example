# Copy to .env. .env is gitignored and must never be committed.
# Key holder: Bikram Bhattarai.
OSHC_API_KEY=
OSHC_MODEL=
# Set to 1 for the demo. A cache miss then raises instead of calling out.
OSHC_OFFLINE=0
